server:
  port: 8081
  context-path: /


spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://ip:3356/points?autoReconnect=true
    username: 用户名
    password: 密码

mybatis:
  mapper-locations: classpath:mapping/*.xml
  type-aliases-package: com.xxx.po
  configuration:
    map-underscore-to-camel-case: true
